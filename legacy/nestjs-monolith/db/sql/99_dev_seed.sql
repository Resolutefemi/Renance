-- ============================================================================
--  Renance - 99_dev_seed.sql
--  Reserved for development seed data (e.g. demo exam for CBT Phase 2).
--  Intentionally empty in Phase 0. Never reference this file in production.
-- ============================================================================

BEGIN;
-- Example shape (uncomment when there are tables to seed):
-- INSERT INTO cbt.exams (id, title, duration_minutes)
-- VALUES (gen_random_uuid(), 'Demo JAMB-style mock', 60);
COMMIT;
